# Content

Game content lives here as JSON (and later Yarn) files — the "soul" the C# engine interprets.
Demo scenes, def files, agent profiles, quests, and schedules accumulate per milestone (see `plan/`).

Everything in this tree is validated in CI by `lattice validate content`.
