{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lattice.dev/schemas/navgrid.schema.json",
  "title": "NavGridDef",
  "description": "A walkable grid declared as rows of legend characters (plan/05 \u00A75). The lowest-sorted navgrid ID is the active grid.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique def ID."
    },
    "type": {
      "const": "navgrid"
    },
    "description": {
      "type": "string",
      "description": "One line for humans and LLMs (surfaced by the manifest)."
    },
    "inherits": {
      "type": "string",
      "description": "Parent def ID (same kind); objects deep-merge, scalars override, arrays replace or take {\u0022$append\u0022/\u0022$remove\u0022} patches.",
      "x-lattice-ref": "navgrid"
    },
    "origin": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "World position of the cell (0,0) corner."
    },
    "cellSize": {
      "type": "number"
    },
    "rows": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Row strings, north to south; row index = Z, column = X."
    },
    "legend": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "walkable": {
            "type": "boolean"
          },
          "cost": {
            "type": "number"
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "description": "Character \u2192 cell descriptor. \u0027.\u0027 defaults to walkable cost 1; \u0027#\u0027 to blocked."
    }
  },
  "required": [
    "id",
    "type"
  ]
}