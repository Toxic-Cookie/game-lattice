{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lattice.dev/schemas/season.schema.json",
  "title": "SeasonDef",
  "description": "A season (plan/05 \u00A74): a content layer activated by the calendar. Def redirects are the v1 overlay mechanism (winter resolves loot_forest to loot_forest_winter); weather bias multiplies transition weights; prefabHints are opaque data for hosts.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique def ID."
    },
    "type": {
      "const": "season"
    },
    "description": {
      "type": "string",
      "description": "One line for humans and LLMs (surfaced by the manifest)."
    },
    "inherits": {
      "type": "string",
      "description": "Parent def ID (same kind); objects deep-merge, scalars override, arrays replace or take {\u0022$append\u0022/\u0022$remove\u0022} patches.",
      "x-lattice-ref": "season"
    },
    "redirects": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Def ID \u2192 replacement def ID while this season is active."
    },
    "weatherBias": {
      "type": "object",
      "additionalProperties": {
        "type": "number"
      },
      "description": "Weather state ID \u2192 weight multiplier applied to Markov transitions."
    },
    "prefabHints": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Engine-side content-swap hints (opaque to the simulation)."
    }
  },
  "required": [
    "id",
    "type"
  ]
}