{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lattice.dev/schemas/need.schema.json",
  "title": "NeedDef",
  "description": "A decaying motive (the Sims pattern, ch05 \u00A75.4): value 1 = satisfied, 0 = desperate; urgency = 1 \u2212 value. Decayed every tick by the agent system for agents whose profile declares the need.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique def ID."
    },
    "type": {
      "const": "need"
    },
    "description": {
      "type": "string",
      "description": "One line for humans and LLMs (surfaced by the manifest)."
    },
    "inherits": {
      "type": "string",
      "description": "Parent def ID (same kind); objects deep-merge, scalars override, arrays replace or take {\u0022$append\u0022/\u0022$remove\u0022} patches.",
      "x-lattice-ref": "need"
    },
    "key": {
      "type": "string"
    },
    "initial": {
      "type": "number"
    },
    "decayPerSecond": {
      "type": "number"
    }
  },
  "required": [
    "id",
    "type"
  ]
}